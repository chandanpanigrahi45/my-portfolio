# Port the Express server listens on
PORT=5000

# Optional: configure SMTP to receive contact form messages by email.
# If these are left blank, submissions are still accepted and logged
# to the server console, but no email is sent.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=chandanpanigrahi916@gmail.com
SMTP_PASS=xgwg tpdl lrzi dwkf

# Where contact form messages should be sent (defaults to SMTP_USER)
CONTACT_RECEIVER=chandanpanigrahi916@gmail.com
